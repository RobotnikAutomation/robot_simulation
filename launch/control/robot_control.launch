<?xml version="1.0"?>
<launch>

  <arg name="robot_id" default="$(optenv ROBOT_ID robot)"/>
  <arg name="prefix" default="$(arg robot_id)_"/>
  <arg name="robot_model" default=""/>

  <!-- Common parameters -->
  <arg name="kinematics" default=""/>
  <arg name="wheel_diameter" default=""/>
  <arg name="track_width" default=""/>
  <arg name="wheel_base" default=""/>
  <arg name="odom_frame" default="$(arg prefix)odom"/>
  <arg name="base_frame" default="$(arg prefix)base_footprint"/>
  <arg name="cmd_vel" default="robotnik_base_control/cmd_vel"/>
  <arg name="cmd_vel_unsafe" default="robotnik_base_control/cmd_vel_unsafe"/>
  <arg name="controller" default="robotnik_base_control"/>
  <arg name="odom_broadcast_tf" default="false"/>
  <arg name="ros_planar_move_plugin" default="false"/>
  <arg name="robot_has_safety" default="false"/>
  
  <!-- Only used by rb1_base and theron -->
  <arg name="has_elevator" default="false"/>
  <arg name="robot_has_brakes" default="false"/>

  <!-- Load generic robot control params-->
  <rosparam file="$(find robot_sim)/config/control/kinematics/$(arg kinematics)_control.yaml" command="load" subst_value="true"/>
  <!-- Load specific robot control params -->
  <rosparam file="$(find robot_sim)/config/control/profiles/$(arg robot_model)_profile.yaml" command="load" subst_value="true"/>

  <!-- Load kinematic controller -->
  <node name="controller_spawner" pkg="controller_manager" type="spawner" respawn="false" output="screen" args="
	  $(arg controller)
    joint_read_state_controller
    ">
  </node>

  <!-- Load twist mux -->
  <node pkg="twist_mux" type="twist_mux" name="twist_mux">
    <rosparam command="load" file="$(find robot_sim)/config/control/twist_mux.yaml" />
    <remap if="$(arg robot_has_safety)" from="cmd_vel_out" to="$(arg cmd_vel_unsafe)" />
    <remap unless="$(arg robot_has_safety)" from="cmd_vel_out" to="$(arg cmd_vel)" />
  </node>

</launch>
